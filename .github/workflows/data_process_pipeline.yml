name: Csv Data Processing
on:
  workflow_dispatch:
  push:
    branches:
    - main
  pull_request:

    
jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Hello
        run: python -c "print('Hello world')"


      - name: Run Python Script
        run: python app.py
